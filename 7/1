def alternate_sum(list1, list2):
    max_len = max(len(list1), len(list2))
    result = [0] * max_len

    for i in range(len(list1)):
        result[i] += list1[i]

    for i in range(len(list2)):
        result[i] += list2[i]

    return result
